//To your normal bot code, add the following lines
const enmap = require('enmap');
//npm i enmap

//Assuming you defined your bot client as `client`
client.settings = new enmap({ name: `Settings`})